#include<stdio.h>
#include<conio.h>
int array(int x)
{
    int i,n;



}
void main()
{
    int x,i,n;
    printf("Enter the size of the array=");
    scanf("%d",&x);
    printf("\nEnter the elements in the array=");
    for(i=1;i<=x;i++)
         scanf("%d",&n);
    printf("The elements in the array are=%d",array(i));

}
